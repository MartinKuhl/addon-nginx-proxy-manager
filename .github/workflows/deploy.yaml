---
name: Deploy

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  #workflow_run:
  #  workflows: ["CI"]
  #  branches: [main]
  #  types:
  #    - completed

jobs:
  workflows:
    uses: MartinKuhl/ha-workflows/.github/workflows/addon-deploy.yaml@0e05a3b9f0f50a1040d30feed6a68f0c5adc73a9 # main